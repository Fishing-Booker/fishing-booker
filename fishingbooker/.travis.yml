sudo: required
language: java
jdk: oraclejdk11

services:
  - mysql

before_install:
  - chmod +x mvnw

addons:
  sonarcloud:
  organization: Fishing-Booker
  token: $SONAR_TOKEN

script:
  - ./mvnw clean install -DskipTests=false -B
  - ./mvnw sonar:sonar -Dsonar.projectKey=$PROJECT_KEY -Dsonar.organization=Fishing-Booker -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN

dist: trusty
